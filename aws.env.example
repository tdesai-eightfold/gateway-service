# AWS deployment: set NGINX_RESOLVER to your VPC DNS server.
# VPC DNS is typically at the base of your VPC CIDR + 2.
# Examples: 10.0.0.2 (for 10.0.0.0/24), 172.16.0.2 (for 172.16.0.0/16)
NGINX_RESOLVER=10.0.0.2

# Optional: use HTTP when TLS is terminated at ALB/NLB
# GATEWAY_HTTPS=0
